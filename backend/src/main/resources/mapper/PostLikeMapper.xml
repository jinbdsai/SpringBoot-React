<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"
        "http://mybatis.org/dtd/mybatis-3-mapper.dtd">

<mapper namespace="com.example.backend.like.dao.PostLikeDao">

    <select id="countByPostIdAndUsername" resultType="int">
        SELECT COUNT(*)
          FROM post_likes
         WHERE post_id = #{postId}
           AND username = #{username}
    </select>

    <insert id="insertLike" parameterType="PostLikeVO">
        INSERT INTO post_likes (post_id, username, created_at)
        VALUES (#{postId}, #{username}, NOW())
    </insert>

    <delete id="deleteByPostIdAndUsername">
        DELETE FROM post_likes
         WHERE post_id = #{postId}
           AND username = #{username}
    </delete>

</mapper>
